#include <stdio.h>
/**
* main - Entry point
* @void: return no value it true
*
* Return: Always 0 (Success)
*
* Program to list all base 10 numberz
*/
	int main(void)

{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
	putchar(i);
	}
	putchar('\n);

	return (0);
}
