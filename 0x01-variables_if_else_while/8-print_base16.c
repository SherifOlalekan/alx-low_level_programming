#include <stdio.h>
/**
* main - entry point
* @void: return no value if true
*
* Return: always zero
*
*Program to print lower case of base 16
*/
	int main(void)

{
	int ch;

	{
	for (ch = 48; ch <= 58; ch++)
	putchar (ch);

	for (ch = 97; ch <= 103; ch++)
	putchar (ch);
	}
	putchar ('\n');

	return (0);
}
