#include <stdio.h>
/**
* main - entry point
* @void: return no value if true
*
* Return: always zero
*
*Program to print lower case of base 16
*/
	int main(void)

{
	int ch;

	{
	for (ch = 0; ch <= 10; ch++)
	puchar (ch);

	for (ch = '61'; ch <= 67; ch++)
	putchar (ch);
	}
	putchar ('\n');

	return (0);
}
